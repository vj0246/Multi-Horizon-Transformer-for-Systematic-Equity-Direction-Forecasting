# Actual dependencies, derived from the imports in Source/, scripts/ and tests/.
# Install this and every documented command runs.

# ---- core pipeline (required) ----
numpy
pandas
scipy
scikit-learn
pyyaml
joblib
tensorflow==2.21.0        # 2.11+ is CPU-only on native Windows; see Documentation/02
yfinance                  # market data download

# ---- tests ----
pytest

# ---- optional: gradient-boosted baselines (scripts/gbdt_*.py) ----
lightgbm

# ---- optional: read-only artifact API (Source/Api/main.py) ----
fastapi
uvicorn

# ---- optional: news sentiment track, OFF by default (Source/News/) ----
# NewsAPI only serves ~30 days, so this cannot be backfilled - see Documentation/03
requests
transformers              # FinBERT
torch                     # FinBERT backend
